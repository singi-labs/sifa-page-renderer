---
"@singi-labs/sifa-page-renderer": patch
---

Two personal-site tweaks: drop the shipped brand logo (it was an inaccurate hand extraction) so every activity card icon uses the category glyph, and remove the duplicate `@handle` line under the sidebar name (the handle already appears as the Bluesky link in the links section).
