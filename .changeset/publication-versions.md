---
"@singi-labs/sifa-page-renderer": patch
---

Fold the versions of one publication into a single entry: a preprint or an older release is listed under the lead publication as a "Preprint" / "Other version" link, using the SDK's `groupPublicationVersions`. Bumps `@singi-labs/sifa-sdk` to 0.19.33.
