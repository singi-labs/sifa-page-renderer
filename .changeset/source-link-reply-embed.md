---
"@singi-labs/sifa-page-renderer": patch
---

Activity stream: link each card to its record on the origin app (a "View on {source}" affordance in the meta row, opening in a new tab) and render replies with a "Replying to" caption above the embedded original post. Bumps the `@singi-labs/sifa-sdk` peer to 0.12.15 for `StreamCardVM.sourceUrl`.
